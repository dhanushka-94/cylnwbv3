<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Asset Version
    |--------------------------------------------------------------------------
    |
    | This version number is appended to all asset URLs (CSS, JS, images)
    | to force browsers to fetch fresh files when you increment it.
    | Increment this version from the admin panel when you want visitors
    | to see the latest updates immediately.
    |
    */

    'version' => env('ASSET_VERSION', time()),

];

